<h1><%= @student.to_s %></h1>
<%if @student.active == false%>

  <p>This student is currently inactive.</p>
    <%= link_to Activate, student_path(@student)/activate %>
<%else%>
  <p>This student is currently active.</p>
    <%= link_to Deactivate, student_path(@student)/activate %>
<%end%>
